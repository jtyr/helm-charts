apiVersion: v1
kind: Service
metadata:
  name: {{ template "libchart.v1.fullname" . }}
  labels: {{- include "libchart.v1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
  selector: {{- include "libchart.v1.selectorLabels" . | nindent 4 }}
